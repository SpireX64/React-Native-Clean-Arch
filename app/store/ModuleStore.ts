import { ContainerModule, interfaces } from "inversify";

export const moduleStoreTypes = Object.freeze({

});

const moduleStore = new ContainerModule((bind: interfaces.Bind) => {

});

export default moduleStore;
