import { StyleSheet } from "react-native";
import { INDENT_SM } from "../../../../shared/design/indents";

const styles = StyleSheet.create({
  contentWrapper: {
    flex: 1,
  },
  loadingWrapper: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default styles;
