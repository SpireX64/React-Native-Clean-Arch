import { ContainerModule, interfaces } from "inversify";

export const moduleGatewayTypes = Object.freeze({

});

const moduleGateway = new ContainerModule((bind: interfaces.Bind) => {

});

export default moduleGateway;
