import { ContainerModule, interfaces } from "inversify";

export const moduleNativeTypes = Object.freeze({

});

const moduleNative = new ContainerModule((bind: interfaces.Bind) => {

});

export default moduleNative;
